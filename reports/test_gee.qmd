---
jupyter:
  jupytext:
    formats: ipynb,../reports//qmd
    text_representation:
      extension: .qmd
      format_name: quarto
      format_version: '1.0'
      jupytext_version: 1.19.3
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

```{python}
# Run this cell to authenticate with GEE
# When prompted :
#   select `Cloud Project: imars-simm-belize`
#   "Generate Token"
#   "Continue" past safety warning
#   Check boxes & "Continue"
#   Paste the code into the box below & press enter
ee.Authenticate(auth_mode="notebook")
```

```{python}
# Run this cell to test GEE.
# An Image ID should be printed.
import ee

ee.Initialize(project="imars-simm-belize")
img = ee.ImageCollection("COPERNICUS/S2_SR_HARMONIZED").first()

print("Image ID:")
print(img.get("PRODUCT_ID").getInfo())
```

